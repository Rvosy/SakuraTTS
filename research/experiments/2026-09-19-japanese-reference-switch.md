# 实际更换日文参考与请求状态隔离

日期：2026-09-19。本轮使用同一个“朱雀院红叶”V2Pro 模型和原始三句日文，验证两个真实参考条件之间的切换。目标是检查条件准备、身份绑定和请求状态；没有修改目标句子，也没有把两参考的自由采样差异当成计算错误。

## 参考条件

A 沿用 `VO02_0204.OGG`，原转录为 `じゃあ、私、もっと悪い子になっちゃおうな〜`。B 选用原资源 `ref.txt` 中已有转录的 `VO02_00087.ogg`：`篁組の副担任を任された件ですか?`。B 为 44.1 kHz 单声道、141120 帧，即 3.2 秒，满足官方参考时长检查；未改写转录。

B 音频 SHA-256 为 `3f17868d2f6baf6376c10cb92cb87661a686b733fcf17902a3badcc9522400e4`。独立准备工具从原始音频、原转录及固定官方资源生成新包，实际子进程退出码 0，导出后独立重载的五数组字节相同，输入和源码身份检查通过。没有把 B 与 A 作相等性验收。

新包为 `models/converted/20260919T152440.202799Z-v2pro-japanese-reference/`，manifest SHA-256 为 `cf9bdc23d09d5d37b121b8bffca4a42c2f3f14d0fccf2155d78312ab7694c650`。原模型、原录音和历史实验不变。

## 常驻模型切换

用三个独立 worker 分别执行 A、B、A→B→A，每条请求使用 NumPy seed 1234。后者三次调用保持同一 GPT / SoVITS 实例，每次重新 Prefill，生成后通过公开接口释放请求 KV。每个 worker 从原始目标文本准备日文前端条件，没有向产品注入历史目标数组。

先确认 A、B 的参考音素、prompt semantic、ge 和 ge512 都确实不同，再检查每次模型实际收到的五组参考数组与本次包逐位一致。不能只根据包路径或 identity 不同判断参考已经切换。

三个 worker 退出码均为 0，合计五次请求。常驻序列的三条结果分别与对应独立 worker 比较，15 组数组逐字节相同；规范化文本、停止原因、返回 index 和 WAV 整文件相同，共 57 项对照通过。A→B→A 中最后一个 A 恢复了最初 A 的输出。

| 参考 | 生成语义 token | 主体时长 | 正常停止 | WAV SHA-256 |
|---|---:|---:|---|---|
| A | 110 | 4.40 s | argmax_eos、sample_eos | `03d9d67b21b46fcd2e611f2eae5acd040228a5d24f6106ba325299ea9463e987` |
| B | 115 | 4.60 s | argmax_eos、sample_eos | `91a276f8085b3d4ee4aebc0b451d29d37e42fadab95daef31200a92e4b5b1b8c` |

每次释放请求 KV 并清理空闲缓存后，常驻权重的 MLX active 为 469.05 MiB、cache 为 0；各 worker 完整卸载后 active / cache 均为 0，RSS 仍约 733–741 MiB。这里的诊断含实际输入捕获和同步，没有据此报告速度改善或逐请求峰值，也没有宣称长期无泄漏。

## 独立官方对照

随后由 `research/tools/official_reference_capture.py` 从 B 的原始录音和转录调用固定官方 `TTS.run`。它只从准备记录读取原始输入路径与哈希，不加载已导出的参考数组，也不调用 SakuraTTS 的参考准备或声学适配器。初始化跳过日文不使用的中文 BERT，实际日文前端、参考编码、采样和生成仍使用官方方法，非日文语言段明确拒绝。该运行保存所有实际导入的官方源码并与固定提交核对，官方工作目录状态保持不变。

捕获真实语义采样 draw、声学 `randn_like` 输出和实际 ge/ge512，再从原始目标文本调用自有运行时。B 包的五数组与独立官方观测逐字节相同，目标与拼接音素 / BERT、sampled token、history、停止原因、返回 index、semantic 均通过对照，最终波形最大绝对误差为 `1.557e-5`，通过原有 `atol=1e-4, rtol=1e-5` 容差。未重新评估全部逐层中间量或采样概率，不据此取消旧数值失败记录。

官方实际进程退出码 0。加入官方回放的第二轮 native Harness 四个 worker 全部退出 0，前三个 worker 又验证了一轮 A→B→A，官方 B 回放则生成 114 个语义 token、4.56 秒主体音频。官方 Torch seed 与自有 NumPy seed 不是同一随机序列，因此这个 114 不能拿去和上表自由采样 B 的 115 直接作数值比较。

## 内容检查

新 seed 1234 的两条独立输出另外通过离线 Whisper-small 检查，不提供目标转录提示，强制日文：

- A：`こんにちは。今日は良い天気ですね。よろしくお願いします。`
- B：`こんにちは今日はいい天気ですね よろしくお願いします`

ASR 都识别到了开头和三句内容。它不验证助词 wa、音色或自然度；这两份新音频尚无人工试听结论，不继承之前其他文件的听感评价。另已向用户提供独立官方 B 与固定随机输入的自有 B 两条样音，等待新的试听反馈；该对照不能沿用自由采样 ASR 结论。

## 证据与范围

路径相对于 `SakuraTTS-References/`：

- `runs/20260919T152245.312870Z-japanese-reference-b-selection/`：原转录位置、时长与准备命令。
- `runs/20260919T152440.202799Z-japanese-reference-prepare/`：真实参考准备、进程退出及来源检查。
- `runs/20260919T154031.363002Z-native-reference-switch/`：冻结源码、完整命令、输入哈希、三 worker 的中间数组、WAV、原始日志与比较。
- `runs/20260919T154608.547186Z-official-reference-capture/`：独立官方 B 推理、真实随机数和实际条件，子进程退出码 0。
- `runs/20260919T154828.961833Z-native-reference-switch/`：第二轮参考切换和官方 B 随机输入回放，四个 worker 退出码均为 0。
- `runs/20260919T154205.896851Z-reference-switch-asr-dispatch/` 和 `runs/20260919T154205.927723Z-asr-review/`：ASR 的真实进程退出、模型身份和原始识别结果。

当前只覆盖两个日文参考和一条原始日文目标。分别切换单参考不等于同时组合多个参考；其他参考语言、其他模型和 Windows / CUDA 仍待验证。诊断代码位于 `research/tools/native_reference_switch.py`。
