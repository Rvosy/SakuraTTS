# 历史研究材料

`tools/` 保存专项诊断与回放脚本，`experiments/` 保存原报告和已引用的数据。这些文件不进入运行 wheel；源码包保留完整证据。

常用性能与官方对照从 `benchmarks/` 进入，结论索引在 `docs/research/`。新的输出不要继续写进此目录，使用被 Git 忽略的 `outputs/`、`results/` 或 `artifacts/`。

历史报告中的设备、模型和绝对来源路径属于当时的记录。路径迁移不会使已有数值失败变成通过；重跑需要准备对应平台及模型资源。
