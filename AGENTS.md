# AGENTS.md

## 目标

理解用户真正的意图，解决问题的根因，并将任务推进到完成。

需求明确时直接行动。只有缺失信息会实质改变结果，或操作具有明显的不可逆 / 外部风险时，才需要确认。

## 项目知识

按任务需要查阅相关资料，不要默认读取全部文档。

* `docs/specs/`：当前行为、契约与不变量。
* `docs/adr/`：重要架构决策及原因。
* `tests/` / `benchmarks/`：产品行为的可执行验证。
* `research/`：实验、历史记录与对应证据。

Spec 说明**什么必须保持成立**；ADR 说明**为什么这样设计**。

普通 Bug 通常只需要修复和回归验证。只有长期行为契约变化时更新 Spec，只有产生重要且长期的架构取舍时更新 ADR。

## 开发

先理解相关代码和现有行为，再修改。

可以跨模块调查和修改，以解决真正的根因；不预设文件范围或实现路径。

保持改动与任务相关，避免无关重写。

尊重用户已有修改，不要为了获得“干净状态”而覆盖或丢弃它们。

如果现有设计看起来复杂，先确认是否存在 Spec、ADR、兼容性要求或历史原因，再决定是否简化。

### 保持简单

* 新增抽象、校验、兼容层、重试、状态或限制，必须有具体消费者、真实失败场景或明确契约；否则采用直接实现。
* 同一规则由真正负责它的边界处理。内部调用遵循已建立的契约，不重复校验、转换、缓存或维护同一状态。
* 删除已无消费者的旧路径和重复实现；保留数据安全、权限、资源回收、取消等真实边界。失败保留原因，恢复复用已有路径，不吞错伪装成功。

## 验证

根据改动的实际影响选择验证范围。

优先运行相关的局部测试或产品 Harness；涉及共享基础设施、跨模块行为或存在较大风险时，再扩大验证范围。

可以直接执行安全的本地构建、测试和静态检查，修复本次修改造成的问题并重新验证。

测试用户可观察行为和真实风险，不锁定源码形状、函数排列或整套内部字段；同一风险优先在最有代表性的一层验证。

没有实际运行过的检查，不要声称已经通过。

## Git

分支使用 `feat/`、`fix/`、`refactor/`、`docs/`、`test/` 或 `chore/` 前缀。

提交信息使用 Conventional Commits，描述部分用中文写明具体变化，例如 `fix: 修复取消后未回收的推理进程`。一个提交表达一个完整意图。

不要把无关修改混入同一提交，也不要为了制造细粒度历史而机械拆分紧密相关的修改。

除非用户明确要求，不要执行 force push、历史重写或其他破坏性 Git 操作。

## 文本

编写或修改面向人的中文文本时使用 `humanizer-zh` skill，包括文档、UI、提示、日志、注释、模板和 Git / PR 说明。语言自然、直接、具体，保留技术含义和既有术语。

* 正式产物使用项目作者或产品本身的口吻，只说明事实、行为、原因、结果和必要操作；不混入 Agent 对话、授权说明、任务汇报或思考过程。真实业务中的审批、等待状态照常说明。
* 每份文档职责明确，同一事实保留一个详细来源，其他位置具体引用；代码拥有的字段、默认值和限制引用对应文件与符号。README 保留介绍和快速上手，细节进入指南；修改生成文本时同步模板、生成器和对应版本的副本。
* 正式内容描述当前有效用法，过程与历史记录单独归档。删除空泛免责声明，保留测量条件、适用范围、实际失败和未验证项；区分已实现、已验证、已发布，保留原始证据、冻结快照、许可证与第三方归属。

## 安全

不要泄漏凭据、破坏用户已有工作，或未经明确要求执行破坏性 Git、生产部署、生产数据修改等高风险操作。

普通代码目录和模块边界不是安全边界。

## 完成

完成前检查最终改动，移除意外或无关内容，并确认用户要求已经真正解决。

最终简要说明：

* 修改了什么；
* 实际验证了什么；
* 仍有哪些已知风险或未验证项。

## 维护本文件

`AGENTS.md` 只保留长期、项目级、确实有必要的规则。

不要因为 Agent 曾经犯过一次错误就增加永久规则。优先通过代码结构、Spec、ADR 和测试解决问题。

模型能力、项目结构或工作方式变化后，应删除已经失去必要性的指令。
