# AGENTS.md

## 目标

理解用户真正的意图，解决问题的根因，并将任务推进到完成。

需求明确时直接行动。只有缺失信息会实质改变结果，或操作具有明显的不可逆 / 外部风险时，才需要确认。

保持实现简单，避免不必要的抽象、兼容层和流程复杂度。

## 项目知识

按任务需要查阅相关资料，不要默认读取全部文档。

* `docs/specs/`：当前行为、契约与不变量。
* `docs/adr/`：重要架构决策及原因。
* `tests/` / `harness/`：现有行为的可执行验证。

Spec 说明**什么必须保持成立**；ADR 说明**为什么这样设计**。

普通 Bug 通常只需要修复和回归验证。只有长期行为契约变化时更新 Spec，只有产生重要且长期的架构取舍时更新 ADR。

## 开发

先理解相关代码和现有行为，再修改。

可以跨模块调查和修改，以解决真正的根因；不预设文件范围或实现路径。

保持改动与任务相关，避免无关重写。

尊重用户已有修改，不要为了获得“干净状态”而覆盖或丢弃它们。

如果现有设计看起来复杂，先确认是否存在 Spec、ADR、兼容性要求或历史原因，再决定是否简化。

## 验证

根据改动的实际影响选择验证范围。

优先运行相关的局部测试或产品 Harness；涉及共享基础设施、跨模块行为或存在较大风险时，再扩大验证范围。

可以直接执行安全的本地构建、测试和静态检查，修复本次修改造成的问题并重新验证。

不要为了形式增加与实现重复、价值有限的测试。

没有实际运行过的检查，不要声称已经通过。

## Git

分支使用清晰的类型前缀：

* `feat/xxx` — 新功能
* `fix/xxx` — Bug 修复
* `refactor/xxx` — 重构
* `docs/xxx` — 文档
* `test/xxx` — 测试
* `chore/xxx` — 工程维护

提交信息使用 Conventional Commits 风格，**描述部分使用中文**：

```text
feat: 增加插件自动发现能力
fix: 修复 Core 重启后插件未恢复的问题
refactor: 简化 Runtime 生命周期所有权
docs: 更新插件架构说明
test: 增加插件生命周期回归测试
chore: 更新开发工具配置
```

提交信息应简洁描述“这次提交完成了什么”，避免模糊描述，例如：

```text
update
fix bug
修改代码
一些优化
```

一个提交应表达一个清晰、完整的意图。

不要把无关修改混入同一提交，也不要为了制造细粒度历史而机械拆分紧密相关的修改。

除非用户明确要求，不要执行 force push、历史重写或其他破坏性 Git 操作。

## 文本

编写或修改面向人的中文文本时，使用 `humanizer-zh` skill 进行规范，包括：

* README / 文档
* Spec / ADR
* PR / Issue 说明
* Release Notes / Changelog
* 用户可见文案
* 较长的代码注释
* Git 提交信息

目标是让文本自然、直接、具体，减少模板化、机械化和明显的 AI 写作痕迹。

不要为了“humanize”而改变技术含义、事实或已有术语。

## 安全

不要泄漏凭据、破坏用户已有工作，或未经明确要求执行破坏性 Git、生产部署、生产数据修改等高风险操作。

普通代码目录和模块边界不是安全边界。

## 完成

完成前检查最终改动，移除意外或无关内容，并确认用户要求已经真正解决。

最终简要说明：

* 修改了什么；
* 实际验证了什么；
* 仍有哪些已知风险或未验证项。

## 维护本文件

`AGENTS.md` 只保留长期、项目级、确实有必要的规则。

不要因为 Agent 曾经犯过一次错误就增加永久规则。优先通过代码结构、Spec、ADR 和测试解决问题。

模型能力、项目结构或工作方式变化后，应删除已经失去必要性的指令。
